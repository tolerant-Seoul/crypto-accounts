---
name: commit-clean
description: This skill should be used when users want to commit changes grouped by feature with production-quality workflow. Includes pre-validation (protected branches, sensitive files, sync check), semantic analysis of git diff, Conventional Commits format, Breaking Change detection, and proper error handling. No Co-Author attribution. Triggered by "커밋해줘", "기능별로 커밋", "commit and push", "clean commit".
---

# Commit Clean

Git 변경사항을 분석하여 기능별로 분리 커밋하고 푸시합니다.
Conventional Commits 규격을 준수하며, Co-Author를 추가하지 않습니다.

## Arguments

| 인자 | 설명 |
|------|------|
| (none) | 기능별 커밋 + 푸시 |
| `--no-push` | 커밋만, 푸시 안 함 |
| `--dry-run` | 계획만 출력, 실행 안 함 |
| `--interactive` | 각 커밋 전 확인 요청 |
| `--amend` | 마지막 커밋에 추가 (주의 필요) |

---

## Phase 1: 사전 검증

### 1.1 Git 상태 확인

```bash
git status --porcelain
git rev-parse --abbrev-ref HEAD
git remote -v
```

**검증 항목:**
- [ ] Git 저장소인가?
- [ ] 현재 브랜치 확인
- [ ] 리모트 설정 확인

### 1.2 보호 브랜치 체크

**main/master 직접 푸시 경고:**
```
⚠️ 현재 브랜치: main
직접 푸시하시겠습니까? (보통 PR을 권장합니다)
[Y/n]
```

### 1.3 원격 동기화 확인

```bash
git fetch origin
git status -uno
```

**Behind 상태면:**
```
⚠️ 원격에 새 커밋이 있습니다.
먼저 pull 하시겠습니까? [Y/n]
```

### 1.4 충돌 및 문제 파일 검사

```bash
git diff --check  # 충돌 마커, trailing whitespace
```

**민감 파일 감지 (커밋 차단):**
```
.env, .env.*, *.pem, *.key, *credentials*, *secret*
id_rsa, id_ed25519, *.p12, *.pfx, serviceAccount*.json
```

발견 시:
```
🚨 민감 파일 감지됨:
  - .env.local
  - credentials.json

이 파일들은 커밋에서 제외됩니다.
.gitignore에 추가하시겠습니까? [Y/n]
```

### 1.5 대용량 파일 경고

100KB 이상 파일:
```
⚠️ 대용량 파일:
  - assets/video.mp4 (15MB)

Git LFS 사용을 고려하세요. 계속하시겠습니까? [Y/n]
```

---

## Phase 2: 변경 분석

### 2.1 변경 파일 수집

```bash
# Staged
git diff --cached --name-status

# Unstaged
git diff --name-status

# Untracked
git ls-files --others --exclude-standard
```

**출력 파싱:**
- `A` = Added (새 파일)
- `M` = Modified (수정)
- `D` = Deleted (삭제)
- `R` = Renamed (이름 변경)
- `??` = Untracked (미추적)

### 2.2 각 파일 상세 분석

**각 파일에 대해:**

```bash
# 변경 통계
git diff --stat <file>

# 실제 diff (최대 500줄)
git diff <file> | head -500
```

**분석 항목:**

| 항목 | 확인 방법 |
|------|----------|
| 새 함수/클래스 추가 | `+pub fn`, `+fn `, `+class `, `+def ` |
| 함수 시그니처 변경 | 기존 함수의 파라미터/리턴타입 변경 |
| 버그 수정 | 조건문 수정, 에러 핸들링 추가 |
| 리팩토링 | 이름 변경, 구조 개선 (동작 동일) |
| 의존성 변경 | import/use 문 변경 |
| 설정 변경 | config 파일 수정 |
| 린트/포맷 | 공백, 세미콜론, 정렬만 변경 |
| 테스트 | test 디렉토리 또는 _test, .test 파일 |
| 문서 | *.md, 주석, docstring |

### 2.3 Breaking Change 감지

**Breaking Change 패턴:**

```
- pub fn 이름 변경
- 함수 파라미터 제거/타입 변경
- public API 삭제
- 필수 설정 추가
- 데이터 스키마 변경
```

발견 시 커밋 메시지에 반영:
```
feat(api)!: 인증 방식 변경

BREAKING CHANGE: Bearer 토큰 대신 API Key 사용
기존 토큰은 더 이상 유효하지 않습니다.
```

---

## Phase 3: 논리적 그룹핑

### 3.1 그룹핑 규칙

**같은 커밋으로 묶을 조건:**

1. **같은 기능/모듈**
   - `src/evm/mod.rs` + `src/evm/types.rs` + `tests/evm_test.rs`

2. **같은 이슈/티켓**
   - 파일에 주석으로 이슈 번호가 있는 경우

3. **연관된 변경**
   - 인터페이스 변경 + 해당 구현체 수정
   - 함수 추가 + 해당 테스트 추가

**반드시 분리할 조건:**

1. 서로 다른 기능 (feat + feat → 분리)
2. 기능 추가 + 버그 수정 (feat + fix → 분리)
3. 관련 없는 리팩토링
4. Breaking change는 별도 커밋

### 3.2 커밋 순서

권장 순서:
1. `chore`: 의존성, 설정 변경
2. `refactor`: 리팩토링 (기능 변경 없음)
3. `feat`: 새 기능
4. `fix`: 버그 수정
5. `test`: 테스트
6. `docs`: 문서

---

## Phase 4: Conventional Commits 메시지 생성

### 4.1 메시지 포맷

```
<type>(<scope>): <subject>

[body]

[footer]
```

### 4.2 Type 결정

| Type | 사용 시점 | SemVer |
|------|----------|--------|
| `feat` | 새 기능 추가 | MINOR |
| `fix` | 버그 수정 | PATCH |
| `docs` | 문서만 변경 | - |
| `style` | 코드 의미 변경 없음 (포맷팅) | - |
| `refactor` | 버그 수정도 기능 추가도 아닌 코드 변경 | - |
| `perf` | 성능 개선 | PATCH |
| `test` | 테스트 추가/수정 | - |
| `build` | 빌드 시스템, 외부 의존성 | - |
| `ci` | CI 설정 | - |
| `chore` | 기타 (src/test 미변경) | - |
| `revert` | 이전 커밋 되돌림 | - |

**Breaking Change:**
- type 뒤에 `!` 추가: `feat!:`, `fix!:`
- 또는 footer에 `BREAKING CHANGE:` 추가

### 4.3 Scope 결정

**프로젝트 구조 기반:**

```
src/
├── bip39.rs      → scope: bip39
├── bip32.rs      → scope: bip32
├── evm/          → scope: evm
├── bitcoin/      → scope: bitcoin
├── solana/       → scope: solana
└── lib.rs        → scope: core 또는 생략

web/              → scope: web
.github/          → scope: ci
```

**Monorepo의 경우:**
```
packages/
├── core/         → scope: core
├── cli/          → scope: cli
└── web/          → scope: web
```

### 4.4 Subject 작성 규칙

- **50자 이내** (엄격)
- **명령형** 사용: "추가한다" (X) → "추가" (O)
- **마침표 없음**
- **무엇을, 왜** 담기

**좋은 예:**
```
feat(evm): EIP-55 체크섬 주소 지원
fix(bip39): 체크섬 비트 순서 오류 수정
refactor(bip32): 키 도출 로직 단순화
```

**나쁜 예:**
```
feat: 수정함.                    # 모호, 마침표
feat(evm): mod.rs 파일 수정      # 파일명은 의미없음
feat: EIP-55 체크섬 주소 지원 기능을 추가하였습니다  # 너무 김
```

### 4.5 Body 작성 (선택)

**언제 필요한가:**
- Subject만으로 설명 부족
- 변경 이유 설명 필요
- 구현 방식 선택 이유

**포맷:**
```
feat(evm): EIP-55 체크섬 주소 지원

Keccak-256 해시 기반 mixed-case 인코딩 구현.
체크섬 적용으로 주소 오타 감지 가능.

- 대소문자로 4비트 체크섬 인코딩
- 해시 니블이 8 이상이면 대문자
```

### 4.6 Footer 작성 (선택)

**이슈 연결:**
```
Closes #123
Fixes #456
Refs #789
```

**Breaking Change:**
```
BREAKING CHANGE: 기존 API 호환되지 않음

마이그레이션 방법:
1. config.json의 apiVersion을 2로 변경
2. authenticate() 대신 login() 사용
```

**Co-Author (이 스킬에서는 사용 안 함):**
```
# 이 라인은 추가하지 않음
Co-Authored-By: Name <email>
```

---

## Phase 5: 커밋 실행

### 5.1 Staged 초기화

```bash
git reset HEAD 2>/dev/null || true
```

### 5.2 그룹별 커밋

```bash
# 파일 추가
git add <file1> <file2> ...

# 커밋 (HEREDOC으로 멀티라인)
git commit -m "$(cat <<'EOF'
<type>(<scope>): <subject>

<body>

<footer>
EOF
)"
```

### 5.3 커밋 검증

```bash
# 커밋 성공 확인
git log -1 --oneline

# pre-commit hook 실패 시
# → 에러 메시지 출력
# → 해당 커밋 건너뛰기 또는 중단 선택
```

### 5.4 실패 시 롤백

```bash
# 마지막 커밋 취소 (staged 유지)
git reset --soft HEAD~1
```

---

## Phase 6: 푸시

### 6.1 푸시 전 확인

```bash
git log origin/<branch>..<branch> --oneline
```

```
📤 푸시할 커밋:
  abc1234 feat(evm): EVM 계정 생성 구현
  def5678 fix: clippy 경고 수정

총 2개 커밋을 origin/main에 푸시합니다.
```

### 6.2 푸시 실행

```bash
git push origin <branch>
```

### 6.3 푸시 실패 처리

**Rejected (non-fast-forward):**
```
⚠️ 원격에 새 변경이 있어 푸시가 거부되었습니다.

옵션:
1. git pull --rebase 후 다시 푸시
2. 중단

선택: [1/2]
```

---

## Phase 7: 결과 리포트

### 7.1 성공 시

```
✅ 커밋 완료

┌─────────────────────────────────────────────────────┐
│ 1. feat(evm): EVM 계정 생성 구현                      │
│    └─ src/evm/mod.rs (+227, -2)                     │
│                                                      │
│ 2. fix: clippy 경고 수정                              │
│    ├─ src/bip39.rs (+0, -1)                         │
│    └─ src/bip32.rs (+0, -1)                         │
│                                                      │
│ 3. docs: 프로젝트 문서 업데이트                         │
│    ├─ CLAUDE.md (+150, -18)                         │
│    └─ notes/bip-39_learn.md (+30, -0)              │
└─────────────────────────────────────────────────────┘

🚀 Pushed to origin/main (3 commits)
   https://github.com/97woo/crypto-accounts/commits/main
```

### 7.2 --dry-run 출력

```
📋 커밋 계획 (dry-run)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

그룹 1: feat(evm)

  파일:
    A  src/evm/mod.rs (+227 lines)

  분석:
    - EvmAccount 구조체 정의
    - from_mnemonic(), from_seed() 메서드
    - private_key_to_public_key() 함수
    - public_key_to_address() 함수 (Keccak-256)
    - to_checksum_address() 함수 (EIP-55)
    - 테스트 3개 포함

  커밋 메시지:
    ┌────────────────────────────────────────────┐
    │ feat(evm): EVM 계정 생성 구현               │
    │                                            │
    │ - secp256k1 공개키에서 주소 도출            │
    │ - Keccak-256 해시 적용                     │
    │ - EIP-55 체크섬 주소 지원                  │
    └────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

그룹 2: fix

  파일:
    M  src/bip39.rs (-1 line)
    M  src/bip32.rs (-1 line)

  분석:
    - bip39: 미사용 import `hmac::Hmac` 제거
    - bip32: 불필요한 참조 `&sha256_hash` → `sha256_hash`

  커밋 메시지:
    ┌────────────────────────────────────────────┐
    │ fix: clippy 경고 수정                       │
    │                                            │
    │ - bip39: 미사용 import 제거                │
    │ - bip32: 불필요한 borrow 제거              │
    └────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

요약: 2개 커밋 예정
실행하려면: /commit-clean
```

---

## 프로젝트별 커스터마이징

### .commitlintrc 지원 (있는 경우)

프로젝트 루트에 `.commitlintrc.json` 또는 `commitlint.config.js`가 있으면 해당 규칙 적용.

### .commit-clean.json (선택적 설정)

```json
{
  "language": "ko",
  "scopes": ["core", "evm", "bitcoin", "web"],
  "maxSubjectLength": 50,
  "requireScope": false,
  "requireBody": false,
  "protectedBranches": ["main", "master", "release/*"],
  "sensitivePatterns": [
    ".env*",
    "*secret*",
    "*.pem"
  ]
}
```

---

## 에러 처리

| 상황 | 대응 |
|------|------|
| 변경 없음 | "커밋할 변경사항이 없습니다" 출력 후 종료 |
| pre-commit 실패 | 에러 표시, 수정 후 재시도 안내 |
| 충돌 마커 발견 | 커밋 차단, 충돌 해결 안내 |
| 민감 파일 | 해당 파일 제외, .gitignore 추가 제안 |
| 푸시 거부 | pull --rebase 제안 |
| 권한 오류 | SSH/HTTPS 인증 확인 안내 |
